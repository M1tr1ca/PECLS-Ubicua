comunicación bidireccional (recoge los datos del sensor y envía los datos que recoge la esp anterior)
tiene que tener un enpoint al que conectarnos desde Android.
En log4j están puestas las rutas de linux, deben cambiarse a Windows.

cambiar el war

webapps, conf para poool de conexiones, context.xmml, estrcutura, aqui hayq que configurar la pool de conexiones en context.xml